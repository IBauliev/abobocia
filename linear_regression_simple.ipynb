## Ответы на вопросы

1. Аналитическое решение: w = (X^T X)^-1 X^T y

2. L2 регуляризация добавляет lambda * w^2 к потере
   L1 регуляризация добавляет lambda * |w| к потере

3. L1 обнуляет веса неважных признаков

4. Для нелинейных зависимостей добавляем полиномы признаков
